<div class="media">
  <div class="media-body">
    <h4 class="media-heading"><%= comment.author_name %>
      <small>Posted <%= distance_of_time_in_words(comment.created_at, Time.now) %> early</small>
    </h4>
    <%= comment.body %>
  </div>
</div>
